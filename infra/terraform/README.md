# Dokeza Terraform Baseline

This directory is the provider-neutral Terraform root used by CI until the first cloud provider is selected.

It intentionally defines no cloud resources yet. The goal is to keep Terraform formatting and validation wired before production infrastructure modules are introduced.
